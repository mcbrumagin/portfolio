
main > ul {
    
    @if (window[width] > 400) {
        li[width] == window[width] * 1/2;
    }
    
    @if (window[width] > 800) {
        li[width] == window[width] * 1/4;
    }
    
    li {
        width: == window[width] * 3/4;
    }
}
